import { GetZoomPositionDirective } from './get-zoom-position.directive';

describe('GetZoomPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new GetZoomPositionDirective();
    expect(directive).toBeTruthy();
  });
});
